<svg xmlns="http://www.w3.org/2000/svg" width="400" height="400" viewBox="0 0 400 400">
  <!-- Yellow background on left side -->
  <rect x="0" y="0" width="200" height="400" fill="#FFFF00" />

  <!-- Pink background on right side -->
  <rect x="200" y="0" width="200" height="400" fill="#FF00FF" />

  <!-- Face placeholder (user should replace this image) -->
  <circle cx="200" cy="170" r="120" fill="#00FFFF" fill-opacity="0.3" />

  <!-- Red turtleneck/shirt area -->
  <rect x="80" y="280" width="240" height="120" fill="#FF0000" />

  <!-- Text indicator -->
  <text x="200" y="200" font-family="Arial, sans-serif" font-size="24"
        fill="#000000" text-anchor="middle" font-weight="bold">
        @pasternak neon image
  </text>
  <text x="200" y="230" font-family="Arial, sans-serif" font-size="16"
        fill="#000000" text-anchor="middle">
        (Replace with actual image)
  </text>
</svg>
