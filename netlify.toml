[build]
  command = "npm install && npm run build"
  publish = "dist"
  environment = { NODE_VERSION = "20.18.0" }

[dev]
  command = "npm run dev"
  port = 8888
  framework = "vite"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
